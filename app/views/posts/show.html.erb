<div>
  <% @posts.each do |p| %>
    <div class="post">
      <div class="post-profile">
        <span class="post-username"><%= link_to p.user.name, p.user %></span>
        <span class="post-numberofposts"><%= "Hozzászólások száma: #{p.user.posts.count}" %></span>
        <span class="post-joined"><%= "Csatlakozott: #{p.user.created_at.to_date}" %></span>
      </div>

      <div class="post-date">
        <%= p.created_at %>
      </div>

      <div class="post-content">
        <%= p.text %>
      </div>
    </div>
  <% end %>
</div>